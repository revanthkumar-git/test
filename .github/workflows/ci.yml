name: StudyFlow CI Pipeline

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  backend-test-and-build:
    name: Backend Tests & Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: 'StudyFlow/backend/package-lock.json'

      - name: Install Backend Dependencies
        working-directory: ./StudyFlow/backend
        run: npm ci

      - name: Generate Prisma Client & Database
        working-directory: ./StudyFlow/backend
        env:
          DATABASE_URL: file:./dev.db
        run: |
          npx prisma generate
          npx prisma db push

      - name: Run Backend Integration Tests
        working-directory: ./StudyFlow/backend
        env:
          DATABASE_URL: file:./dev.db
        run: npm test

      - name: Build Backend TypeScript
        working-directory: ./StudyFlow/backend
        run: npm run build

  frontend-build:
    name: Frontend Build & Typecheck
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: 'StudyFlow/frontend/package-lock.json'

      - name: Install Frontend Dependencies
        working-directory: ./StudyFlow/frontend
        run: npm ci

      - name: Build Frontend Application
        working-directory: ./StudyFlow/frontend
        run: npm run build